/* eslint-disable */

import React from 'react';
import styled from 'styled-components';

const StyledInfo = styled.div({});

const Info = ({
  displayTime,
}) => {
  return (
    <StyledInfo>
      {displayTime}
    </StyledInfo>
  );
};

export default Info;


